# Plantilla para desarrollo local (copia a .env.local si quieres overrides sin tocar .env del repo).
# El .env del repo ya apunta a Render (TestFlight / producción).

API_BASE_URL=http://127.0.0.1:3000
SOCKET_URL=http://127.0.0.1:3001
FLUTTER_WEB_PORT=8088
ENABLE_DEV_TOOLS=true
# DEV_HOST=192.168.1.42
# TUNNEL_PREFIX=
