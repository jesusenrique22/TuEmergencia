# Local Flutter SDK checkout (do not commit; use system Flutter)
/flutter/

# Uploaded patient medical files (local dev)
backend/uploads/

# Miscellaneous
*.class
*.log
.dev-*.log
build_error*.txt
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
Thumbs.db

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# VS Code (local settings; commit shared launch/tasks/settings)
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json
!.vscode/extensions.json

# Flutter / Dart / Pub
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
**/coverage/
*.g.dart.bak

# Flutter web build (scripts/serve-web-tunnel.sh, run-web-lan.sh)
build/web/

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android build outputs
/android/app/debug
/android/app/profile
/android/app/release
/android/.gradle/
*.apk
*.aab

# iOS / macOS build artifacts
*.ipa
*.dSYM.zip
*.dSYM

# Rutas locales opcionales (copia desde config/local-paths.env.example)
config/local-paths.env
!config/local-paths.env.example
android/local.properties
!android/local.properties.example

# Environment / secrets (never commit real values)
.env
.env.*
!**/.env.example
.env.local
.env.*.local

# Signing keys (never commit)
key.properties
**/*.keystore
**/*.jks

# pnpm store at repo root (if created locally)
.pnpm-store/

# Node.js — backend, realtime-gateway, and any service folder
**/node_modules/
**/dist/
**/package-lock.json
**/yarn.lock

# TypeScript build info
*.tsbuildinfo

# CocoaPods
/ios/Pods/
/macos/Pods/

# Keep Android Gradle wrapper for reproducible builds
!android/gradlew
!android/gradlew.bat
!android/gradle/wrapper/gradle-wrapper.jar

# Python (scripts/serve_web_no_cache.py)
__pycache__/
*.py[cod]
.Python
.venv/
venv/

# Docker local overrides
docker-compose.override.yml
**/docker-compose.override.yml

# Local databases / dumps (dev only)
*.sqlite
*.sqlite3
*.db
dump.rdb

# Cursor / local tooling
.cursor/

# OS / editor backups
*~
*.orig
